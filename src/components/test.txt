.cardWrapper {
  display: flex; /* Возвращаем flexbox для более простого управления */
  justify-content: center; /* Центрируем по горизонтали */
  align-items: center; /* Центрируем по вертикали */
  position: relative;
  width: 100%;
  height: 100vh; /* Занимаем всю высоту вьюпорта */
  padding-bottom: 50px; /* Добавляем отступ снизу, чтобы поднять блоки */
  box-sizing: border-box; /* Учитываем padding в общей высоте */
}

.cardLeft {
  background: #302f3a;
  /* Увеличиваем насыщенность тени */
  box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.4); /* Увеличиваем смещение, размытие и непрозрачность */
  border-radius: 24px 0px 0px 24px;
  padding: 24px;
  color: #fff;
  height: 84vh;
  width: 100%; /* Занимаем всю доступную ширину */
  max-width: 600px; /* Ограничиваем максимальную ширину */
  z-index: 5;
  margin-left: 50px;
  position: relative;
  transform: translateY(-20px); /* Поднимаем левую карточку немного вверх */
}

.cardRigth {
  background: #302f3a;
  /* Увеличиваем насыщенность тени для правой карточки */
  box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.4); /* Добавляем тень и сюда */
  border-radius: 24px; /* Закругляем углы для правой карточки */
  height: 90vh;
  width: 100%;
  max-width: 500px; /* Ограничиваем максимальную ширину */
  z-index: 1; /* Правая карточка будет под левой */
  margin-left: -50px;
  transform: translateY(20px); /* Опускаем правую карточку немного вниз, для эффекта наложения */
}

/* Header */
.headerImageWrapper {
  overflow: hidden;
  margin-bottom: 16px;
  height: 200px;
  border-radius: 16px;
}
.headerImage {
  width: 100%;
  height: 100%;
  object-fit: cover;
}
.title {
  font-size: 22px;
  font-weight: 700;
}
.subtitle {
  font-size: 16px;
  color: #aaa;
}
.location,
.dateTime {
  margin-top: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}
.icon {
  color: #3ecd5e;
}

/* Description */
.description {
  margin: 20px 0;
  line-height: 1.5;
}

/* Footer */
.footer {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  align-items: center;
  justify-content: space-between;
}
.payMobile {
  display: flex;
  align-items: center;
  gap: 10px;
  cursor: pointer;
  color: #3ecd5e;
  font-weight: 600;
}
.payMobileIcon {
  width: 24px;
  height: 24px;
}
.organizerContacts {
  font-size: 14px;
  margin-left: auto;
}
.organizerTitle {
  font-weight: bold;
}
.telegramIconWrapper {
  margin-left: auto;
}
.telegramIcon {
  width: 32px;
  height: 32px;
  color: #fff;
}

/* Modal */
.modalOverlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.6);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}
.modalContent {
  max-width: 400px;
  width: 90%;
  background: #2a2a3c;
  padding: 24px;
  border-radius: 16px;
  color: #fff;
  text-align: center;
}
.modalTitle {
  font-size: 18px;
  margin-bottom: 16px;
}
.qrWrapper {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
  margin: 20px 0;
}
.modalButton {
  background: #3ecd5e;
  color: #fff;
  border: none;
  padding: 12px 20px;
  border-radius: 12px;
  font-size: 16px;
  cursor: pointer;
  transition: background 0.3s;
}
.modalButton:hover {
  background: #34b152;
}
.closeButton {
  margin-top: 10px;
  background: transparent;
  border: 1px solid #aaa;
  padding: 8px 16px;
  border-radius: 8px;
  color: #fff;
  cursor: pointer;
}

/* Адаптивность */
@media (max-width: 600px) {
  .modalContent {
    width: 95%;
    padding: 16px;
  }
  .modalButton {
    width: 100%;
  }
}

/* custom scrollbar
-------------------------------------*/
.custom-scroll-wrapper {
  padding-right: 12px;
}

.custom-scroll::-webkit-scrollbar-track {
  border-radius: 0.25rem;
  background-color: white;
}

.custom-scroll.custom-scroll-light::-webkit-scrollbar-track {
  border-radius: 0.25rem;
  background-color: var(--color-shadow);
}

.custom-scroll::-webkit-scrollbar {
  width: 0.5rem;
  background-color: white;
}